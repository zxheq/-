1、学习过程应该创建一个空的虚拟机
2、虚拟机最小内存628M，使用IOS映像（映像的设备状态要已连接，否者无法连接上映像）；
3、小技巧：
	虚拟机主页面有一个表（快照），可以创建快照，快照可以快速读取，还可以
	回档。
	注意：真正的服务器没有快照这个概念，请好好学习，避免误操作
4、克隆：
	克隆是可以在同一份内存上使用多个虚拟机，占用内存小的优点，注意：删除克隆机不会对主机造成影响，
	但是删除主机会造成克隆机不能使用。